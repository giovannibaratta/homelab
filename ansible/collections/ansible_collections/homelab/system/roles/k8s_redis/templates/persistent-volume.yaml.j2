apiVersion: v1
kind: PersistentVolume
# Local PVs are pinned to specific nodes for performance and predictable failover in homelab nodes.
metadata:
  name: local-pv-{{ __k8s_redis_node_name }}-{{ __k8s_redis_cluster_name }}
spec:
  capacity:
    storage: {{ __k8s_redis_storage_size }}
  volumeMode: Filesystem
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: local-storage
  local:
    path: {{ __k8s_redis_storage_path }}
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          # Pin this volume to the specified node
          - {{ __k8s_redis_node_name }}
