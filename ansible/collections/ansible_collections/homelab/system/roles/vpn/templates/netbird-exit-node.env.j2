NB_MANAGEMENT_URL=https://netbird.{{ internal_domain }}:443
NB_ADMIN_URL=https://netbird.{{ internal_domain }}:443
NB_LOG_LEVEL=info
# The key is generated from the Netbird UI. This means that is it not automated
# and if the environment is deployed from scratch, the playbook must be run twice.
# The node is automatically added to a group that will be used as exit nodes.
NB_SETUP_KEY={{ vpn_exit_node_setup_key }}